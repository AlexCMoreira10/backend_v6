<template>
  <div class="q-pa-xs col-xs-12 col-sm-6 col-md-4">
    <q-card>
      <q-card-title>{{ title }}</q-card-title>
      <q-card-section class="text-center">
        {{ queryprops.func }} of {{ queryprops.attribute }}
        <br>
        <strong>{{ title }}</strong>
      </q-card-section>
      <q-separator />
      <q-card-section class="flex flex-center">
        <div>{{ queryResult }} {{ suffix }}</div>
      </q-card-section>
    </q-card>
  </div>
</template>

<script>
import DataLoadMixin from './DataLoadMixin';
export default {
  name: 'DashboardSumItem',
  mixins: [
    DataLoadMixin
  ],
  props: {
    title: String,
    icon: String,
    model: String,
    suffix: String,
    queryprops: Object
  },

  data() {
    return {
      // Add any component data here
      queryResult: null
    };
  },
  async mounted() {
    this.queryResult = await this.query()
    console.log(this.service)
  },
  computed: {

    // async queryResult(){
    //     return this.query(this.queryprops)
    // }
  },
  methods: {
    // Add any component methods here
    async query() {
      return this.service.query(this.queryprops.queryparams, this.queryprops.func)
    }
  },
};
</script>